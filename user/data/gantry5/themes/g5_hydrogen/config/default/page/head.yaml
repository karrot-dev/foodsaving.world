meta: {  }
head_bottom: ''
atoms:
  -
    id: assets-1631
    type: assets
    title: 'Custom CSS / JS'
    attributes:
      enabled: '1'
      css: {  }
      javascript:
        - { location: '', inline: "const hash = window.location.hash\nif (hash.startsWith('#!/')) {\n  window.location.replace('https://karrot.world/#' + hash.slice(2) )\n}", in_footer: '0', extra: {  }, priority: '0', name: 'Redirect old angular app' }
